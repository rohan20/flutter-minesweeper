import 'package:flutter/material.dart';

class GameBoardCoveredMineTile extends StatelessWidget {
  final bool flagged;
  final int posX;
  final int posY;

  GameBoardCoveredMineTile({this.flagged, this.posX, this.posY});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
